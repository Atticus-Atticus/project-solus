extends Node

var Destination: int = 0
#0 means no level when interact with airlock
#1 means change scene to abandoned ship
#2 means change scene to Brimpie Oribtal

#func changeScene(path : String):
	#get_tree().change_scene(path)
